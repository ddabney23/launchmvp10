# ============================================
# OPTIMIX - Environment Variables Template
# ============================================
# INSTRUCTIONS:
# 1. Copy this file to .env.local in the root directory
# 2. Fill in all YOUR_* placeholders with actual values
# 3. Never commit .env.local to version control!

# ============================================
# REQUIRED - SUPABASE CONFIGURATION
# ============================================
# How to get these values:
# 1. Go to https://app.supabase.com
# 2. Select your project
# 3. Go to Settings → API
# 4. Copy Project URL and anon key

NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_KEY_HERE

# How to get Database URL:
# 1. In Supabase Dashboard → Settings → Database
# 2. Copy Connection String (URI format)
# 3. Replace [YOUR-PASSWORD] with your actual database password

DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres

# Optional but recommended for admin operations:
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY_HERE

# ============================================
# REQUIRED - APPLICATION CONFIGURATION
# ============================================
NEXT_PUBLIC_APP_NAME=Optimix
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# PAYMENT INTEGRATION (Required for Production)
# ============================================
# How to get Stripe keys:
# 1. Go to https://dashboard.stripe.com
# 2. Switch to Test mode for development
# 3. Go to Developers → API keys
# 4. Copy Publishable key and Secret key

NEXT_PUBLIC_STRIPE_PUBLIC_KEY=pk_test_YOUR_STRIPE_PUBLISHABLE_KEY
# Alias (also supported): NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_YOUR_STRIPE_SECRET_KEY

# How to get Webhook Secret:
# 1. In Stripe Dashboard → Developers → Webhooks
# 2. Click "Add endpoint"
# 3. URL: https://your-domain.com/api/webhooks/stripe
# 4. Select events: payment_intent.* and charge.refunded
# 5. Copy the Signing secret

STRIPE_WEBHOOK_SECRET=whsec_YOUR_WEBHOOK_SECRET

# ============================================
# STRIPE SUBSCRIPTION PRICE IDs (Required for Subscriptions)
# ============================================
# How to get Stripe Price IDs:
# 1. Go to https://dashboard.stripe.com → Products
# 2. Create 4 recurring subscription products:
#    - Starter Vendor (Free) - $0/month
#    - Growing Vendor (Basic) - $9.99/month
#    - Serious Vendor (Pro) - $29.99/month
#    - Enterprise Vendor (Premium) - $99/month
# 3. Copy the Price IDs (start with "price_")
# 4. Add them below

# Client-side (used in subscription-tiers.ts)
NEXT_PUBLIC_STRIPE_PRICE_FREE=price_YOUR_FREE_PRICE_ID
NEXT_PUBLIC_STRIPE_PRICE_BASIC=price_YOUR_BASIC_PRICE_ID
NEXT_PUBLIC_STRIPE_PRICE_PRO=price_YOUR_PRO_PRICE_ID
NEXT_PUBLIC_STRIPE_PRICE_PREMIUM=price_YOUR_PREMIUM_PRICE_ID

# Server-side (used in webhook handler)
STRIPE_PRICE_FREE=price_YOUR_FREE_PRICE_ID
STRIPE_PRICE_BASIC=price_YOUR_BASIC_PRICE_ID
STRIPE_PRICE_PRO=price_YOUR_PRO_PRICE_ID
STRIPE_PRICE_PREMIUM=price_YOUR_PREMIUM_PRICE_ID

# ============================================
# SHIPPO SHIPPING API (Required for Shipping Labels)
# ============================================
# How to get Shippo API key:
# 1. Sign up at https://goshippo.com
# 2. Go to Settings → API
# 3. Copy your API key (Test mode: "shippo_test_...")
# 4. Add it below

SHIPPO_API_KEY=shippo_test_YOUR_SHIPPO_API_KEY

# Optional: Shippo Webhook Secret (for tracking updates)
SHIPPO_WEBHOOK_SECRET=whsec_YOUR_SHIPPO_WEBHOOK_SECRET

# ============================================
# OPTIONAL - MONITORING & ERROR TRACKING
# ============================================
# Sentry (Error Tracking)
# Get from: https://sentry.io → Settings → Projects → Client Keys (DSN)
NEXT_PUBLIC_SENTRY_DSN=YOUR_SENTRY_DSN_HERE

# Google Analytics
# Get from: https://analytics.google.com → Admin → Data Streams
NEXT_PUBLIC_GA_TRACKING_ID=G-XXXXXXXXXX

# ============================================
# OPTIONAL - PUSH NOTIFICATIONS
# ============================================
# Generate VAPID keys by running:
# npx web-push generate-vapid-keys

NEXT_PUBLIC_VAPID_PUBLIC_KEY=YOUR_VAPID_PUBLIC_KEY
VAPID_PRIVATE_KEY=YOUR_VAPID_PRIVATE_KEY

# ============================================
# OPTIONAL - EMAIL SERVICE (Transactional Emails)
# ============================================
# Resend API Key
# Get from: https://resend.com → API Keys
RESEND_API_KEY=re_YOUR_RESEND_API_KEY

# Sender email address (must be verified in Resend)
FROM_EMAIL=noreply@optimix.com

# ============================================
# OPTIONAL - TWO-FACTOR AUTHENTICATION
# ============================================
# Service name shown in authenticator apps (e.g., Google Authenticator)
NEXT_PUBLIC_2FA_SERVICE_NAME=Optimix

# ============================================
# DEVELOPMENT SETTINGS
# ============================================
# Set to true to use mock data (for development without backend)
NEXT_PUBLIC_USE_MOCK_DATA=false

# Node environment (development, production, test)
NODE_ENV=development

# ============================================
# EXAMPLE FILLED VALUES (for local development)
# ============================================
# These are EXAMPLE values - replace with your actual credentials!
#
# NEXT_PUBLIC_SUPABASE_URL=https://abcdefghijklmnop.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# DATABASE_URL=postgresql://postgres:your-password@db.abcdefghijklmnop.supabase.co:5432/postgres
# NEXT_PUBLIC_STRIPE_PUBLIC_KEY=pk_test_51AbCdEfGhIjKlMnOp...
# STRIPE_SECRET_KEY=sk_test_51AbCdEfGhIjKlMnOp...

# ============================================
# DATABASE MIGRATIONS (Supabase)
# ============================================
# Apply pending migrations (053+) to your remote project:
#
#   npm run supabase:push
#
# Requires SUPABASE_DB_PASSWORD (or DATABASE_URL) matching the Supabase Dashboard
# database password. If push fails (DNS/pooler), paste the Dashboard SQL bundle:
#
#   npm run supabase:migrations:bundle   # regenerates scripts/apply-pending-migrations.sql
#   # Supabase Dashboard → SQL Editor → run scripts/apply-pending-migrations.sql
#
# Verify applied versions:
#   npm run supabase:migration:list
#
# ============================================
# API SMOKE TESTS
# ============================================
# With dev server running (npm run dev):
#   npm run test:api:smoke
#
# Optional authenticated checks — copy session cookie from browser DevTools after sign-in:
#   TEST_SESSION_COOKIE="sb-...-auth-token=..." npm run test:api:smoke


# ============================================
# SECURITY — server-only secrets (never expose to the browser)
# ============================================
# Generate: openssl rand -hex 32
INTERNAL_API_SECRET=your-internal-api-secret-here
INTERNAL_HEALTH_SECRET=your-internal-health-secret-here

# If real credentials were ever committed to git, rotate immediately in Supabase:
# Dashboard → Settings → API → rotate anon + service_role keys and DB password.

# ============================================
# REQUIRED - APPLICATION CONFIGURATION
# ============================================
NEXT_PUBLIC_APP_NAME=Optimix
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# PAYMENT INTEGRATION (Required for Production)
# ============================================
# How to get Stripe keys:
# 1. Go to https://dashboard.stripe.com
# 2. Switch to Test mode for development
# 3. Go to Developers → API keys
# 4. Copy Publishable key and Secret key

NEXT_PUBLIC_STRIPE_PUBLIC_KEY=pk_test_YOUR_STRIPE_PUBLIC_KEY
STRIPE_SECRET_KEY=sk_test_YOUR_STRIPE_SECRET_KEY

# How to get Webhook Secret:
# 1. In Stripe Dashboard → Developers → Webhooks
# 2. Click "Add endpoint"
# 3. URL: https://your-domain.com/api/webhooks/stripe
# 4. Select events: payment_intent.* and charge.refunded
# 5. Copy the Signing secret

STRIPE_WEBHOOK_SECRET=whsec_YOUR_WEBHOOK_SECRET

# ============================================
# OPTIONAL - MONITORING & ERROR TRACKING
# ============================================
# Sentry (Error Tracking)
# Get from: https://sentry.io → Settings → Projects → Client Keys (DSN)
NEXT_PUBLIC_SENTRY_DSN=https://5b4493581a3fe697eee679591d2aa127@o4510338081292288.ingest.us.sentry.io/4510338082144256

# Google Analytics
# Get from: https://analytics.google.com → Admin → Data Streams
NEXT_PUBLIC_GA_TRACKING_ID=G-Z96CPFZ2SB

# ============================================
# OPTIONAL - RATE LIMITING (Upstash Redis)
# ============================================
# Get from: https://console.upstash.com → Create Database → REST API
# Required for production rate limiting (falls back to disabled in dev)
UPSTASH_REDIS_REST_URL=https://your-database.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-token-here

# ============================================
# OPTIONAL - PUSH NOTIFICATIONS
# ============================================
# Generate VAPID keys by running:
# npx web-push generate-vapid-keys

NEXT_PUBLIC_VAPID_PUBLIC_KEY=YOUR_VAPID_PUBLIC_KEY
VAPID_PRIVATE_KEY=YOUR_VAPID_PRIVATE_KEY

# ============================================
# OPTIONAL - EMAIL SERVICE (Transactional Emails)
# ============================================
# Resend API Key
# Get from: https://resend.com → API Keys
RESEND_API_KEY=re_3oQpCjHr_CFKEuu4bJkTxuZM21ZQQz6CA

# Sender email address (must be verified in Resend)
FROM_EMAIL=noreply@optimix.com

# ============================================
# OPTIONAL - TWO-FACTOR AUTHENTICATION
# ============================================
# Service name shown in authenticator apps (e.g., Google Authenticator)
NEXT_PUBLIC_2FA_SERVICE_NAME=Optimix

