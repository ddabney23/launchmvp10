'use client'

// This component has been replaced by the mobile navigation in Navigation.tsx
// Keeping this file to avoid breaking imports, but it returns null
export const BottomNavigation = () => {
  return null;
};

